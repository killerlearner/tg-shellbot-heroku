{
    "name": "tg-shellbot-heroku",
    "description": "Heroku edition of github.com/botgram/shell-bot",
    "keywords": ["shell", "telegram", "docker", "bot"],
    "repository": "https://github.com/ssnjrthegr8/tg-shellbot-heroku.git",
    "stack": "container",
    "env": {
      "BOT_TOKEN": {
        "description": "This is bot auth token, necessary to connect bot to Telegram device.",
        "required":  "true"
      },
      "OWN_ID": {
        "description": "The chat id which will control the bot. Could be a group or a individual",
        "required":  "true"
      },
      "RCLONE_CONFIG": {
        "description": "Base64 encoded rclone config here. ('cat rclone.conf | base64 > rclone_config_base64.txt')"
      }
    }
  }
  
